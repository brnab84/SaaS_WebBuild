{
  "name": "@webforge/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared zod schemas and types (blocks, design tokens, DTOs) used by api, app and renderer.",
  "exports": {
    ".": "./src/index.ts",
    "./blocks": "./src/blocks.ts",
    "./tokens": "./src/tokens.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
